// setting environment variables for secret key, mongodb url, and port
const config = {
    SECRET_KEY: 'test',
    MONGODB_URL: 'mongodb://0.0.0.0:27017/MealMate',
    PORT: 5000
};

export default config;